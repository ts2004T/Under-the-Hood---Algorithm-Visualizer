## Packages
framer-motion | Essential for smooth algorithm visualizations and layout transitions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind CSS classes safely
lucide-react | Beautiful icons for the UI (already in stack but good to confirm)
prismjs | Syntax highlighting for pseudocode

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
